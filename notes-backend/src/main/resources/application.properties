spring.application.name=notes-backend
# Render port dynamically set karega, warna 8080 use karega
server.port=${PORT:8080}

# ✅ Database Configuration (Ab ye Render ke Environment Variables se value lega)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Hibernate Settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 📧 Email Settings (Ye sahi hai, aise hi rehne do abhi ke liye)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mrabhiverma000@gmail.com
spring.mail.password=rvni rzxa ggqn yyls
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ☁️ Cloudinary Keys (Inhein bhi Env Variables se connect kar do taaki image upload chale)
cloudinary.cloud_name=${CLOUD_NAME}
cloudinary.api_key=${API_KEY}
cloudinary.api_secret=${API_SECRET}

# Upload Limit
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB